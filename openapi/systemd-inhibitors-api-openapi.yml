openapi: 3.2.0
info:
  title: systemd-logind (org.freedesktop.login1) Inhibitors API
  version: '1.0'
  summary: D-Bus API of systemd-logind modeled as REST operations.
  description: 'Documentation/contract artifact modeling the `org.freedesktop.login1` D-Bus interface set

    (system bus). systemd-logind tracks user logins, seats, sessions, and inhibitor locks; gates

    power/reboot/sleep operations; manages ACLs for hot-pluggable devices; and exposes inhibitor

    locks so applications can delay shutdown or idle handling.


    Bus name: `org.freedesktop.login1`. Root object path: `/org/freedesktop/login1`.'
  license:
    name: LGPL-2.1-or-later
    url: https://github.com/systemd/systemd/blob/main/LICENSES/LGPL-2.1-or-later.txt
servers:
- url: dbus://system/org.freedesktop.login1
tags:
- name: Inhibitors
paths:
  /inhibitors:
    get:
      tags:
      - Inhibitors
      operationId: ListInhibitors
      summary: List All Inhibitor Locks
      description: Mirrors `ListInhibitors()`.
      responses:
        '200':
          description: Array of inhibitor records.
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Inhibitor'
    post:
      tags:
      - Inhibitors
      operationId: Inhibit
      summary: Acquire An Inhibitor Lock
      description: Mirrors `Inhibit(what, who, why, mode)`. Returns an FD representing the lock.
      requestBody:
        content:
          application/json:
            schema:
              type: object
              required:
              - what
              - who
              - why
              - mode
              properties:
                what:
                  type: string
                  description: Colon-separated list of shutdown, sleep, idle, handle-power-key, handle-suspend-key, handle-hibernate-key, handle-lid-switch.
                who:
                  type: string
                why:
                  type: string
                mode:
                  type: string
                  enum:
                  - block
                  - delay
      responses:
        '200':
          description: Lock acquired (FD returned over D-Bus).
          content:
            application/json:
              schema:
                type: object
                properties:
                  fd:
                    type: integer
components:
  schemas:
    Inhibitor:
      type: object
      properties:
        what:
          type: string
        who:
          type: string
        why:
          type: string
        mode:
          type: string
          enum:
          - block
          - delay
        uid:
          type: integer
        pid:
          type: integer
